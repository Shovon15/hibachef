import { footerData } from "./footer";
import { gridData } from "./grid";
import { headerData } from "./header";

import { servicePageData } from "./pages/service";

const PAGES = {

  header: headerData,
  footer: footerData,
  grid: gridData,
  galleryData: servicePageData
};

export default PAGES;
